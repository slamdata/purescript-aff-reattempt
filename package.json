{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "pulp build",
    "test": "pulp test",
    "bower": "bower"
  },
  "devDependencies": {
    "pulp": "^11.0.0",
    "purescript": "^0.11.4",
    "rimraf": "^2.6.1",
    "bower": "^1.8.0"
  }
}
